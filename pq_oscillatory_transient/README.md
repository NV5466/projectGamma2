# pq_oscillatory_transient

Power-quality seed folder for oscillatory transient evidence.

The current bundle was assigned here because it uses ensemble Hankel / pole clustering evidence and WC2-style reference outputs. This classification should remain `needs_review` until the transient subtype boundaries are reviewed.

Boundary: synthetic research prototype only; no field calibration claim.
