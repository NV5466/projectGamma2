# relay_coil_inductive_kick Notes

Version 0.2.0 was uploaded as a standalone Python package layout with a boundary harness.

The seed remains distinct from contact bounce, missed short pulse, and generic noise signatures. Boundary material is kept in this folder so the classifier can be reviewed against those negative classes without collapsing the seeds.
